{# comment/index.html.twig #}
<div class="comment mb-5" id="comment-{{ comment.id }}">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">
                <span class="comment-author text-primary">{{ comment.user.username|capitalize }}</span>
                {{ comment.createdAt|date('d M Y à H:i:s') }}
            </h5>
            <p class="card-text">{{ comment.content }}</p>
        </div>
    </div>
</div>
